
  # MindfulFeed App Design

  This is a code bundle for MindfulFeed App Design. The original project is available at https://www.figma.com/design/EF7XOQKlBMHkiyTHT0J1yQ/MindfulFeed-App-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  